<%- include('../layout', { body: `
  <h1>${item.title}</h1>
  <img src="${item.image}" alt="${item.title}" class="item-image">
  <p>${item.description}</p>
  <a href="/items">← Back to Collection</a>
` }) %>
